import { StartDateDirective } from './start-date.directive';

describe('StartDateDirective', () => {
  it('should create an instance', () => {
    const directive = new StartDateDirective();
    expect(directive).toBeTruthy();
  });
});
